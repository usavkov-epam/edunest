{
  "name": "infrastructure",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/infrastructure/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/infrastructure",
        "main": "apps/infrastructure/src/index.ts",
        "tsConfig": "apps/infrastructure/tsconfig.lib.json",
        "assets": ["apps/infrastructure/*.md"]
      }
    }
  }
}
